import{_ as o,c as a,o as e,a2 as t,a6 as n,a7 as i,a8 as d}from"./chunks/framework.CSd21wiM.js";const y=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"usage/domains.md","filePath":"usage/domains.md"}'),s={name:"usage/domains.md"},u=t('<p>By default, all Functions are assigned a unique URL, which is a subdomain of <code>lagoss.com</code>, pointing to the current production Deployment. The subdomain is always the name of the Function: if your Function is named <code>my-function</code>, your unique URL will be <code>my-function.lagoss.com</code>.</p><p>All domains (default and custom) are automatically assigned an SSL certificate, provided by <a href="https://letsencrypt.org/" target="_blank" rel="noreferrer">Let&#39;s Encrypt</a>. They are also automatically renewed before they expire. As such, the first request to a Function using a brand-new custom domain may take a few seconds to generate the SSL certificate.</p><p>All requests to your Function are automatically redirected to HTTPS.</p><h2 id="adding-a-domain" tabindex="-1">Adding a domain <a class="header-anchor" href="#adding-a-domain" aria-label="Permalink to &quot;Adding a domain&quot;">​</a></h2><p>You can add custom domains or subdomains to your Functions, pointing to the current production Deployment. Head over to the settings tab of your Function, and scroll to the &quot;Domains&quot; section. Here, you can see a list including both the default domain and any custom domains or subdomains you&#39;ve added:</p><p><img src="'+n+'" alt="Domains"></p><p>To add a new custom domain or subdomain, click on &quot;Add domain&quot; button at the right, fill in the domain or subdomain and validate. Next, follow the instructions below to <a href="#pointing-your-domain-to-lagoss">point your domain or subdomain to Lagoss</a>.</p><h2 id="pointing-your-domain-to-lagoss" tabindex="-1">Pointing your domain to Lagoss <a class="header-anchor" href="#pointing-your-domain-to-lagoss" aria-label="Permalink to &quot;Pointing your domain to Lagoss&quot;">​</a></h2><p>Now that you&#39;ve added a custom domain or subdomain, you must update the DNS settings of your domain to point to Lagoss. You can do this by adding a CNAME record, pointing to your Function&#39;s default domain.</p><h3 id="for-a-domain" tabindex="-1">For a domain <a class="header-anchor" href="#for-a-domain" aria-label="Permalink to &quot;For a domain&quot;">​</a></h3><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Apex domains (root domains) are not supported. You can redirect your apex domain to a <code>www</code> subdomain, and then point the <code>www</code> subdomain to Lagoss.</p></div><p>Let&#39;s say that you have the following:</p><ul><li>A domain <code>www.domain.com</code></li><li>A Function named <code>awesome-function</code> (which has the default subdomain <code>awesome-function.lagoss.com</code>)</li></ul><p>You should now add a new CNAME record named <code>www</code>, pointing to <code>awesome-function.lagoss.com</code>. After adding this record, go back to the settings tab of your Function, and you should now see your custom domain validated:</p><p><img src="'+i+'" alt="Custom domains"></p><h3 id="for-a-subdomain" tabindex="-1">For a subdomain <a class="header-anchor" href="#for-a-subdomain" aria-label="Permalink to &quot;For a subdomain&quot;">​</a></h3><p>Let&#39;s say that you have the following:</p><ul><li>A subdomain <code>my.domain.com</code></li><li>A Function named <code>awesome-function</code> (which has the default subdomain <code>awesome-function.lagoss.com</code>)</li></ul><p>You should now add a new CNAME record named <code>my</code>, pointing to <code>awesome-function.lagoss.com</code>. After adding this record, go back to the settings tab of your Function, and you should now see your custom subdomain validated:</p><p><img src="'+d+'" alt="Custom domains"></p><h2 id="removing-a-domain" tabindex="-1">Removing a domain <a class="header-anchor" href="#removing-a-domain" aria-label="Permalink to &quot;Removing a domain&quot;">​</a></h2><p>To remove a custom domain or subdomain, click on the &quot;Delete&quot; button next to the domain name. You cannot remove the default subdomain.</p>',22),c=[u];function r(m,l,h,p,g,f){return e(),a("div",null,c)}const w=o(s,[["render",r]]);export{y as __pageData,w as default};
